<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.medyi.whoisup">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_colors"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_colors"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">

        </activity>

        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>

        <activity android:name=".activity.ProfileActivityOther"></activity>
        <activity android:name=".activity.LoginActivity"></activity>

        <activity android:name=".activity.ChatActivity" ></activity>

        <activity android:name=".activity.RoomActivity"></activity>

        <activity android:name=".activity.SignUpActivity"></activity>

        <activity android:name=".activity.ProfileActivity"></activity>

        <activity android:name=".activity.WhoIsUpActivity"></activity>

    </application>

</manifest>